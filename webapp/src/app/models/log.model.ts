import type {
    SystemLogEntry,
    SystemLogLevel,
    SystemLogScope,
    SystemLogSource,
} from "@yinuo-ngm/protocol";

export type LogScope = SystemLogScope;

export type LogLevel = SystemLogLevel;

export type LogSource = SystemLogSource;

export type LogLine = SystemLogEntry;
