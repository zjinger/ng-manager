{
  "name": "ng-manager",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rimraf -g \"packages/**/lib\" \"packages/**/dist\" \"packages/**/*.tsbuildinfo\"",
    "compile": "tsc -b tsconfig.build.json",
    "publish:all": "npm run compile && npm publish -w @yinuo-ngm/core --access public && npm publish -w @yinuo-ngm/server --access public && npm publish -w @yinuo-ngm/cli --access public",
    "pack:all": "npm pack ./packages/core && npm pack ./packages/server && npm pack ./packages/cli",
    "build:core": "tsc -b core",
    "build:server": "tsc -b server",
    "build:desktop": "tsc -b desktop",
    "dev:server": "tsx watch server/src/index.ts",
    "dev:desktop": "tsx watch desktop/src/main/index.ts"
  },
  "devDependencies": {
    "rimraf": "^6.1.2",
    "typescript": "^5.9.3"
  },
  "workspaces": [
    "packages/*"
  ]
}
