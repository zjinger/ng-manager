{
  "name": "@ngm/hub",
  "private": true,
  "scripts": {
    "build:web": "npm --prefix ./web run build",
    "build:server": "npm --prefix ./server run build",
    "copy:web": "node ./scripts/copy-web.js",
    "build": "node ./scripts/build-all.js",
    "deploy:prod": "node ./scripts/deploy-server.js --target=prod",
    "deploy:prod:skip-build": "node ./scripts/deploy-server.js --target=prod --skip-build"
  }
}
